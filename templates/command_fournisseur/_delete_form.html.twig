<form method="post" action="{{ path('app_command_fournisseur_delete', {'id': command_fournisseur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ command_fournisseur.id) }}">
    <button class="btn">Delete</button>
</form>
