{% extends 'base.html.twig' %}

{% block title %}Edit Product{% endblock %}

{% block body %}
    <h1>Edit Product</h1>
    
    {% if is_granted("ROLE_ENTREPOT") %}
    <div> il reste {{ product.quantityp }} {{ product.label }}</div>
    
    <button class="btn">{{ button_label|default('Ajouter') }}</button>
   

    <div> {% endif %}</div>

    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_MARKET') %}
    {{ include('product/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('product/_delete_form.html.twig') }}
    {% endif %}


<a href="{{ path('app_product_index') }}">back to list</a>
{% endblock %}
